\documentclass[tikz,border=5mm]{standalone}

\usepackage{tikz}
\usepackage{pgfplots}
\usepgfplotslibrary{patchplots}
\usepackage{pgfplotstable}

\usetikzlibrary{patterns}

% \pgfplotsset{compat=1.3}

% \usepgfplotslibrary{external} 
% \tikzexternalize

\pgfplotsset{compat=newest}

% For generating .pdf
% \usetikzlibrary{external}
% \tikzexternalize

% To generate .ps; should not be needed
% \tikzset{external/system call={latex \tikzexternalcheckshellescape -halt-on-error
% -interaction=batchmode -jobname "\image" "\texsource";
% dvips -o "\image".ps "\image".dvi}}

% Forcing remake of the .pdfs; otherwise, LaTeX will assume that nothing has changed
% \tikzset{external/force remake}

\begin{document}
\thispagestyle{empty}

\begin{tikzpicture}
    \begin{axis}[
        title={$(V_m,p)$-plot: experimental data},
        ylabel={$p$ [Pa]},
        xlabel={$V_m$ [$\frac{\mathrm{m}^3}{\mathrm{mol}}$]},
        legend entries={Dew points, Bubble points},
        legend style={at={(0.98,0.98)},anchor=north east},
        grid = major,
        % scaled y ticks = true,
        xmode = log, 
        % ymin=0, 
        % ymax=260,
        %xmin=1e1, 
        % xmax=240,
        % restrict y to domain=-1e7:1e7,
        %domain=-10:10
        ]
        \addplot[color=cyan, mark=triangle*, only marks] file {dewPoints_VP.dat};
        \addplot[color=magenta, mark=*, only marks] file {bubblePoints_VP.dat};
        % Use draw=gray to define the outline color

        % \draw[black,very thin, xstep=1, ystep=5] (axis cs:1e-4,150) grid (axis cs:8e-4,180);
        % \draw[black, pattern=dots] (axis cs:1.1e-4,162) rectangle (axis cs:7e-4,192);
        % \node[fill=white] at (axis cs:2.8e-4,175) {Iteration area};
    \end{axis}
\end{tikzpicture}

\end{document}